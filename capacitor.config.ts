import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'tegrika_native',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
